kind: Service
apiVersion: v1
metadata:
  #Service name
  name: node-hello
spec:
  selector:
    app: directed-cove-135122
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
